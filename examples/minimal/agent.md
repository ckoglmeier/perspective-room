# Northstar Robotics

Agent-readable fundraising room.

This example is intentionally metadata-only. Source files are not bundled; agents should treat listed materials as external-access-required.

Use `room.json` for structured ingestion and preserve `authored_by`, `status`, and `sources` when summarizing.
